import { catalogProducts } from "../lib/fulfillment";

// Data-only catalog reference for Shopify Admin import planning.
// This file intentionally does not call Shopify Admin APIs or execute product writes.
export { catalogProducts };
